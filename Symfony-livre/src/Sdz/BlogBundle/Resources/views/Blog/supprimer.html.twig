{% extends 'SdzBlogBundle::layout.html.twig' %}

{% block title %}
    Suppression - {{ parent() }}
{% endblock %}

{% block sdzblog_body %}
    <h2>Suppression de l'article</h2>
    <p>Etes vous certain de supprimer l'article "{{ article.titre}}"</p>

    {# on met le slug de l'article dans la route de l'action#}
   <form action="{{ path('sdzblog_supprimer',{'slug':article.slug}) }}" method="post"{{ form_enctype(form) }}  >

        <a href="{{ path('sdzblog_voir',{'slug':article.slug}) }}" class="btn">
            <i class="icon-chevron-left"></i>
                Retour à l'article
        </a>

       {{ form_rest(form) }}
        <input type="submit" value="Supprimer cet article" class="btn btn-danger"/>
   </form>





{% endblock %}